# b13-輸送パートナー詳細（ユーザー）- 设计规范

## 1. 功能概述

### 1.1 文档信息

| 项目 | 内容 |
|------|------|
| **工作表名称** | b13-輸送パートナー詳細（ユーザー） |
| **界面编号** | b13 |
| **对应Base界面** | b12-輸送パートナーマスタ詳細（会社） |
| **文档版本** | v1.0.1 |
| **最后更新** | 2026-02-10|
| **文档状态** | ✅ 已发布 |

### 1.2 功能描述

b13-輸送パートナー詳細（ユーザー）是ベース（总部）侧查看和管理输送合作伙伴用户详细信息的页面。管理者可以通过此页面：

- 查看用户的只读信息（ユーザーID、メール、SMS）
- 管理用户设置（通知送付先、アカウント区分、サブアカウントへの連絡受領）
- 管理サブアカウント设置（支店・営業所名）
- 管理アカウント有効・無効状态
- 从外部システム（ネコーレWEB）同步用户信息

### 1.3 技术架构

本文档基于 **前后端分离架构**，详细技术规范请参考：

👉 **[\_公共技术架构规范.md](../_公共技术架构规范.md)**

| 层级 | 技术 |
|------|------|
| 前端 | React 18.x + TypeScript + Ant Design |
| 后端 | Spring Boot 3.x + Java 17 |
| 数据库 | PostgreSQL 15.x |

### 1.4 核心功能

| 功能 | 描述 | 优先级 |
|------|------|--------|
| ユーザー情報表示 | 显示用户只读信息 | P0 |
| 通知送付先設定 | 通知发送目标设置 | P0 |
| アカウント区分設定 | メイン/サブ账户设置 | P0 |
| サブアカウント連絡設定 | 子账户通知设置 | P1 |
| 支店・営業所名設定 | 分支机构名称设置 | P1 |
| アカウント状態管理 | 账户有效/无效切换 | P0 |
| 外部システム連携 | 与ネコーレWEB联动 | P0 |

### 1.5 适用范围

- 前端开发人员（详情页面组件实现）
- 后端开发人员（用户管理API开发）
- 测试工程师（功能测试）

---

## 2. 页面元素

### 2.1 元素清单

| 序号 | 元素名称 | 元素类型 | 功能说明 | 交互触发 |
|------|----------|----------|----------|----------|
| ⑴ | ページタイトル | Text | 页面标题显示 | 表示 |
| ⑵ | 編集モード切替 | Button | 编辑/查看模式切换 | クリック |
| ⑶ | ユーザー情報セクション | Card | 用户信息只读区域 | 表示 |
| ⑷ | ユーザーID | Text | 用户ID显示 | 表示 |
| ⑸ | メール | Text | 邮箱地址显示 | 表示 |
| ⑹ | SMS | Text | 手机号码显示 | 表示 |
| ⑺ | ユーザー設定セクション | Card | 用户设置区域 | 表示 |
| ⑻ | 通知送付先 | Radio | メール/SMS选择 | ラジオ選択 |
| ⑼ | アカウント区分 | Radio | メイン/サブ选择 | ラジオ選択 |
| ⑽ | サブアカウントへの連絡受領 | Radio | する/しない选择 | ラジオ選択 |
| ⑾ | サブアカウント設定セクション | Card | 子账户设置区域 | 表示 |
| ⑿ | 支店・営業所名 | Input/Text | 分支机构名称 | 文字入力 |
| ⒀ | アカウント状態セクション | Card | 账户状态区域 | 表示 |
| ⒃ | 有効/無効切替 | Switch | 状态切换开关 | スイッチ |
| ⒄ | 状態説明 | Text | 状态说明文字 | 表示 |
| ⒅ | 戻るボタン | Button | 返回上一页 | クリック |
| ⒆ | 保存ボタン | Primary Button | 保存设置 | クリック |
| ⒇ | キャンセルボタン | Default Button | 取消编辑 | クリック |

### 2.2 フィールド詳細説明

| フィールド | 要素タイプ | 制約 | データ形式 | デフォルト | 編集可否 |
|-----------|-----------|------|-----------|----------|----------|
| ユーザーID | Text | 必須 | 文字列 | - | 閲覧のみ |
| メール | Text | 必須,email形式 | 文字列 | - | 閲覧のみ |
| SMS | Text | 必須,11桁数字 | 数値 | - | 閲覧のみ |
| 通知送付先 | Radio | 必須 | メール/SMS | メール | 編集可 |
| アカウント区分 | Radio | 必須 | メイン/サブ | サブ | 編集可 |
| サブアカウントへの連絡受領 | Radio | 条件必須 | する/しない | - | 編集可 |
| 支店・営業所名 | Input | 条件必須,最大200字 | 文字列 | 空 | 編集可 |
| アカウント有効・無効 | Switch | 必須 | 有効/無効 | 有効 | 編集可 |

### 2.3 条件显示规则

| 条件 | 表示要素 | 非表示要素 |
|------|---------|-----------|
| アカウント区分 = メイン | サブアカウントへの連絡受領 | 支店・営業所名 |
| アカウント区分 = サブ | 支店・営業所名 | サブアカウントへの連絡受領 |
| 新規登録モード | すべての設定項目 | - |

### 2.4 ステータス値詳細

| ステータス値 | 表示名 | タグ色 | 説明 |
|--------------|--------|--------|------|
| 有効 | アカウント有効 | 緑色 | 账户正常使用中 |
| 無効 | アカウント無効 | 赤色 | 账户已禁用 |
| メール | メール | 青色 | 通知发送到邮箱 |
| SMS | SMS | 橙色 | 通知发送到手机 |
| メイン | メインアカウント | 青色 | 主要账户 |
| サブ | サブアカウント | 灰色 | 辅助账户 |

---

## 3. 交互流程

### 3.1 ページロードフロー

| ステップ | アクション | システム応答 |
|----------|-----------|--------------|
| 1 | ユーザーがb13ページへ遷移 | ページ初期化 |
| 2 | URLパラメータからuserId取得 | userId抽出 |
| 3 | モード判定 | new/edit/view |
| 4 | 既存データ取得 | GET /api/v1/partner-users/{userId} |
| 5 | ネコーレWEB情報取得 | GET /api/v1/external/necone-web/users/{userId} |
| 6 | APIレスポンス受信 | 各セクションに表示設定 |
| 7 | ページ表示完了 | モードに応じたUI表示 |

### 3.2 編集モード切替フロー

| ステップ | ユーザー操作 | システム応答 |
|----------|-------------|--------------|
| 1 | 編集ボタンクリック | 編集モードに切り替え |
| 2 | 入力フィールド有効化 | 各セクション入力可 |
| 3 | 保存ボタンクリック | バリデーション実行 |
| 4 | バリデーション成功 | 更新API実行 |
| 5 | 更新成功 | 保存完了メッセージ表示 |
| 6 | 閲覧モードに切り替え | 更新後のデータを表示 |

### 3.3 キャンセルフロー

| ステップ | ユーザー操作 | システム応答 |
|----------|-------------|--------------|
| 1 | キャンセルボタンクリック | 確認ダイアログ表示 |
| 2 | 確認 | 閲覧モードに切り替え |
| 3 | 原始データに復元 | フォーム値をリセット |

### 3.4 アカウント区分変更フロー

| ステップ | ユーザー操作 | システム応答 |
|----------|-------------|--------------|
| 1 | ラジオボタン選択変更 | 条件フィールド表示切替 |
| 2 | メイン→サブ選択 | 連絡受領→非表示、支店名→表示 |
| 3 | サブ→メイン選択 | 支店名→非表示、連絡受領→表示 |

### 3.5 状态切换フロー

| ステップ | ユーザー操作 | システム応答 |
|----------|-------------|--------------|
| 1 | Switchクリック | 確認ダイアログ表示 |
| 2 | 確認 | 更新API実行 |
| 3 | 更新成功 | 状態表示更新、完了メッセージ |

### 3.6 新規登録フロー

| ステップ | ユーザー操作 | システム応答 |
|----------|-------------|--------------|
| 1 | b12から＋新規追加クリック | /b13/new?companyId={companyId} |
| 2 | 会社ID自動設定 | hiddenフィールドに設定 |
| 3 | ネコーレWEB情報取得 | userIdに基づいて情報取得 |
| 4 | フォーム表示 | メール/SMS自動設定 |
| 5 | 各項目入力・選択 | バリデーション |
| 6 | 保存ボタンクリック | 新規登録API実行 |

---

## 4. 界面规范

### 4.1 ページレイアウト

| 属性 | PC端値 | SP端値 | 説明 |
|------|--------|--------|------|
| **ページタイプ** | PCのみ | - | PC专用页面 |
| **セクション配置** | 垂直配置 | 垂直配置 | ユーザー情報→設定→サブアカウント→状態 |
| **カード間隔** | 24px | 16px | カード間余白 |

### 4.2 ユーザー情報セクション仕様

| 属性 | 値 | 説明 |
|------|----|------|
| **セクションタイトル** | ユーザー情報 | 固定标题 |
| **フィールド配置** | 縦配置 | 各フィールド垂直排列 |
| **フィールド間隔** | 16px | フィールド間余白 |

### 4.3 ユーザー設定セクション仕様

| 属性 | 値 | 説明 |
|------|----|------|
| **セクションタイトル** | ユーザー設定 | 固定标题 |
| **ラジオボタン配置** | 垂直配置 | 各オプション垂直排列 |
| **オプショ間隔** | 12px | オプション間余白 |

### 4.4 サブアカウント設定セクション仕様

| 属性 | 値 | 説明 |
|------|----|------|
| **セクションタイトル** | サブアカウント設定 | 固定标题 |
| **入力欄幅** | 300px | PC端固定幅 |

### 4.5 アカウント状態セクション仕様

| 属性 | 値 | 説明 |
|------|----|------|
| **セクションタイトル** | アカウント有効・無効 | 固定标题 |
| **Switch配置** | 左配置 | 开关左对齐 |
| **説明文** | 右配置 | 説明文字右配置 |

### 4.6 ステータスタグ仕様

| ステータス | 背景色 | 文字色 | 説明 |
|-----------|--------|--------|------|
| 有効 | #52c41a | #ffffff | 绿色标签 |
| 無効 | #f5222d | #ffffff | 红色标签 |
| メール | #1890ff | #ffffff | 蓝色标签 |
| SMS | #fa8c16 | #ffffff | 橙色标签 |
| メイン | #1890ff | #ffffff | 蓝色标签 |
| サブ | #d9d9d9 | #000000 | 灰色标签 |

---

## 5. API接口设计

### 5.1 ユーザー情報取得API

```typescript
// GET /api/v1/partner-users/{userId}
interface GetPartnerUserRequest {
  userId: string;
}

interface GetPartnerUserResponse {
  success: boolean;
  data: {
    userId: string;
    email: string;
    personalSms: string;
    notificationTarget: 'メール' | 'SMS';
    accountType: 'メイン' | 'サブ';
    subAccountNotification: 'する' | 'しない' | null;
    branchName: string | null;
    accountStatus: '有効' | '無効';
    companyId: string;
    createdAt: string;
    updatedAt: string;
  };
  error?: {
    code: string;
    message: string;
  };
}
```

### 5.2 ユーザー情報更新API

```typescript
// PUT /api/v1/partner-users/{userId}
interface UpdatePartnerUserRequest {
  userId: string;
  updateData: {
    notificationTarget?: 'メール' | 'SMS';
    accountType?: 'メイン' | 'サブ';
    subAccountNotification?: 'する' | 'しない';
    branchName?: string;
    accountStatus?: '有効' | '無効';
  };
}

interface UpdatePartnerUserResponse {
  success: boolean;
  message: string;
}
```

### 5.3 ユーザー新規作成API

```typescript
// POST /api/v1/partner-users
interface CreatePartnerUserRequest {
  userId: string;
  companyId: string;
  notificationTarget: 'メール' | 'SMS';
  accountType: 'メイン' | 'サブ';
  subAccountNotification?: 'する' | 'しない';
  branchName?: string;
  accountStatus: '有効' | '無効';
}

interface CreatePartnerUserResponse {
  success: boolean;
  data: {
    userId: string;
    createdAt: string;
  };
  message: string;
}
```

### 5.4 ネコーレWEB情報取得API

```typescript
// GET /api/v1/external/necone-web/users/{userId}
interface GetNeconeWebUserResponse {
  success: boolean;
  data: {
    userId: string;
    email: string;
    phone: string;
    branchName?: string;
    lastLoginAt?: string;
  };
  error?: {
    code: string;
    message: string;
  };
}
```

### 5.6 画面-API-数据库关系映射表

#### 5.6.1 关系总览表

| 画面元素 | API接口 | 接口功能说明 | 相关数据库表 | 字段关系 |
|----------|---------|--------------|--------------|----------|
| ユーザーID | GET /api/v1/partner-users/{userId} | 获取用户ID | m_partner_users | user_id → userId (只读) |
| メール | GET /api/v1/partner-users/{userId} | 获取邮箱地址 | m_partner_users | email → email (只读) |
| SMS | GET /api/v1/partner-users/{userId} | 获取手机号码 | m_partner_users | personal_sms → personalSms (只读) |
| 通知送付先 | PUT /api/v1/partner-users/{userId} | 更新通知发送目标 | m_partner_users | notification_target ← updateData.notificationTarget |
| アカウント区分 | PUT /api/v1/partner-users/{userId} | 更新账户区分 | m_partner_users | account_type ← updateData.accountType |
| サブアカウントへの連絡受領 | PUT /api/v1/partner-users/{userId} | 更新子账户通知设置 | m_partner_users | sub_account_notification ← updateData.subAccountNotification |
| 支店・営業所名 | PUT /api/v1/partner-users/{userId} | 更新分支机构名称 | m_partner_users | branch_name ← updateData.branchName |
| アカウント有効・無効 | PUT /api/v1/partner-users/{userId} | 更新账户状态 | m_partner_users | account_status ← updateData.accountStatus |
| ＋新規追加 | POST /api/v1/partner-users | 创建新用户 | m_partner_users | 字段直接映射 |
| ネコーレWEB情報 | GET /api/v1/external/necone-web/users/{userId} | 获取外部系统信息 | 外部系统 | 数据同步 |

#### 5.6.2 关系说明

**页面加载流程**：
1. 用户从b12页面点击用户ID链接进入b13页面
2. URL参数解析获取 userId（新建模式时为 new）
3. 判断页面模式：view/edit/new
4. 既存用户模式：调用用户信息API和ネコーレWEB信息API
5. 新规用户模式：获取companyId参数，调用ネコーレWEB信息API
6. API从m_partner_users获取用户数据
7. 页面根据accountType条件显示/隐藏子账户相关字段

**数据表关系（ER图）**：

```
┌─────────────────────────────────────────────────────────────┐
│                    m_partner_users                           │
├─────────────────────────────────────────────────────────────┤
│ PK: user_id                                                  │
│ FK: company_id ─────────────────────► m_partner_companies   │
│    email                                                     │
│    personal_sms                                             │
│    notification_target                                      │
│    account_type                                             │
│    sub_account_notification                                 │
│    branch_name                                               │
│    account_status                                           │
│    password_hash                                            │
│    last_login_at                                            │
│    created_at                                               │
│    updated_at                                               │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ 1:N
                              ▼
┌─────────────────────────────────────────────────────────────┐
│              m_partner_user_audit_log                        │
├─────────────────────────────────────────────────────────────┤
│ PK: id                                                       │
│ FK: user_id ─────────────────────► m_partner_users          │
│    action                                                   │
│    before_value                                             │
│    after_value                                              │
│    performed_by                                             │
│    performed_at                                             │
└─────────────────────────────────────────────────────────────┘
```

**字段映射逻辑**：
- m_partner_users.notification_target (ENUM) ↔ API notificationTarget ('メール'|'SMS')
- m_partner_users.account_type (ENUM) ↔ API accountType ('メイン'|'サブ')
- m_partner_users.sub_account_notification (ENUM) ↔ API subAccountNotification ('する'|'しない')
- m_partner_users.account_status (ENUM) ↔ API accountStatus ('有効'|'無効')

**条件显示规则**：
- accountType = 'メイン' → 显示 subAccountNotification，隐藏 branchName
- accountType = 'サブ' → 显示 branchName，隐藏 subAccountNotification

#### 5.6.3 数据流图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              用户操作层                                       │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐              │
│  │ ｶｳﾝﾄ区分 │    │ 通知送付先 │    │ 状態切替  │    │ 保存/取消│              │
│  │  Radio   │    │  Radio   │    │  Switch  │    │  Button  │              │
│  └────┬─────┘    └────┬─────┘    └────┬─────┘    └────┬─────┘              │
└───────┼──────────────┼──────────────┼──────────────┼───────────────────────┘
        │              │              │              │
        ▼              ▼              ▼              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           React Frontend                                      │
│  ┌───────────────────────────────────────────────────────────────────────┐   │
│  │                  PartnerUserDetailPage.tsx                             │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────────┐    │   │
│  │  │ UserInfo    │  │UserSetting  │  │  AccountStatusSection      │    │   │
│  │  │ (只读)      │  │(可编辑)     │  │  (Switch)                  │    │   │
│  │  └─────────────┘  └─────────────┘  └─────────────────────────────┘    │   │
│  │                          Conditional Rendering                         │   │
│  │                    (根据accountType显示/隐藏字段)                      │   │
│  └───────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
        │              │              │
        │ GET/PUT     │ POST         │ GET (外部)
        ▼              ▼              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         REST API Layer                                        │
│  ┌───────────────────────────────────────────────────────────────────────┐   │
│  │  GET /api/v1/partner-users/{userId}                                  │   │
│  │  PUT /api/v1/partner-users/{userId}                                   │   │
│  │  POST /api/v1/partner-users                                           │   │
│  │  GET /api/v1/external/necone-web/users/{userId}                      │   │
│  └───────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
        │              │              │
        │ SQL Query    │ SQL Insert   │
        ▼              ▼              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Database Layer (PostgreSQL)                         │
│  ┌────────────────────────────────────────┐  ┌────────────────────────────┐  │
│  │       m_partner_users                  │  │  m_partner_user_audit_log  │  │
│  │       (用户主表)                        │  │  (用户操作审计日志表)     │  │
│  └────────────────────────────────────────┘  └────────────────────────────┘  │
│         ▲                                      │                           │
│         │ FK                                   │ FK                         │
│         │                                      ▼                           │
│  ┌────────────────────────────────────────┐                                 │
│  │      m_partner_companies               │                                 │
│  │      (公司主表)                         │                                 │
│  └────────────────────────────────────────┘                                 │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 5.6.4 核心数据库表结构

**m_partner_users（合作伙伴用户表）**

| 字段名 | 数据类型 | 约束 | 默认值 | 说明 |
|--------|----------|------|--------|------|
| user_id | VARCHAR(50) | PK, NOT NULL | - | 用户ID（主键） |
| company_id | VARCHAR(50) | FK → m_partner_companies, NOT NULL | - | 所属公司ID |
| email | VARCHAR(200) | NOT NULL, UNIQUE | - | 邮箱地址 |
| personal_sms | VARCHAR(20) | NOT NULL | - | SMS电话号码 |
| notification_target | ENUM('メール','SMS') | NOT NULL | 'メール' | 通知送付先 |
| account_type | ENUM('メイン','サブ') | NOT NULL | 'サブ' | 账户区分 |
| sub_account_notification | ENUM('する','しない') | NULL | - | サブアカウントへの連絡受領 |
| branch_name | VARCHAR(200) | NULL | - | 支店・営業所名 |
| account_status | ENUM('有効','無効') | NOT NULL | '有効' | 账户状态 |
| password_hash | VARCHAR(255) | NULL | - | 密码哈希 |
| last_login_at | TIMESTAMP | NULL | - | 最后登录时间 |
| created_at | TIMESTAMP | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | NOT NULL | CURRENT_TIMESTAMP | 更新时间 |

**m_partner_user_audit_log（用户操作审计日志表）**

| 字段名 | 数据类型 | 约束 | 默认值 | 说明 |
|--------|----------|------|--------|------|
| id | BIGSERIAL | PK | - | 日志ID |
| user_id | VARCHAR(50) | FK → m_partner_users, NOT NULL | - | 用户ID |
| action | VARCHAR(50) | NOT NULL | - | 操作区分 |
| before_value | TEXT | NULL | - | 变更前值 |
| after_value | TEXT | NULL | - | 变更后值 |
| performed_by | VARCHAR(100) | NOT NULL | - | 执行者 |
| performed_at | TIMESTAMP | NOT NULL | CURRENT_TIMESTAMP | 执行时间 |

**关联关系说明**：
- `m_partner_users` 与 `m_partner_companies` 为 N:1 关系（多个用户属于一个公司）
- `m_partner_user_audit_log` 与 `m_partner_users` 为 N:1 关系（一个用户有多个操作日志）
- 删除用户时，通过级联删除自动删除关联的审计日志记录

```typescript
interface ApiErrorResponse {
  success: false;
  error: {
    code: ErrorCode;
    message: string;
    details?: Record<string, unknown>;
  };
}

type ErrorCode =
  | 'USER_NOT_FOUND'
  | 'INVALID_USER_ID'
  | 'INVALID_EMAIL'
  | 'INVALID_PHONE_NUMBER'
  | 'INVALID_REQUEST'
  | 'DUPLICATE_USER'
  | 'PERMISSION_DENIED'
  | 'EXTERNAL_SYSTEM_ERROR'
  | 'SYSTEM_ERROR';
```

---

## 6. 前端实现

### 6.1 メインコンポーネント

```typescript
// PartnerUserDetailPage.tsx
import React, { useState, useEffect } from 'react';
import { useParams, useNavigate, useSearchParams } from 'react-router-dom';
import {
  Card,
  Form,
  Input,
  Button,
  Radio,
  Switch,
  message,
  Space,
  Divider,
  Typography,
  Alert,
} from 'antd';
import {
  EditOutlined,
  SaveOutlined,
  CloseOutlined,
  ArrowLeftOutlined,
  SyncOutlined,
} from '@ant-design/icons';
import { PartnerUser, CreatePartnerUserData } from '../../types/partnerUser';
import { getPartnerUser, updatePartnerUser, createPartnerUser } from '../../api/partnerUser';
import { getNeconeWebUser } from '../../api/external';
import styles from './PartnerUserDetailPage.module.css';

const { Title, Text, Paragraph } = Typography;

type Mode = 'view' | 'edit' | 'new';

export const PartnerUserDetailPage: React.FC = () => {
  const { userId } = useParams<{ userId: string }>();
  const [searchParams] = useSearchParams();
  const navigate = useNavigate();
  const [form] = Form.useForm();
  
  const mode: Mode = userId === 'new' ? 'new' : 'edit';
  const companyId = searchParams.get('companyId');
  
  const [loading, setLoading] = useState(mode !== 'new');
  const [saving, setSaving] = useState(false);
  const [data, setData] = useState<PartnerUser | null>(null);
  const [isEditing, setIsEditing] = useState(mode === 'new');
  const [neconeWebInfo, setNeconeWebInfo] = useState<any>(null);

  const accountType = Form.useWatch('accountType', form);

  useEffect(() => {
    if (mode === 'new') {
      if (companyId) {
        form.setFieldsValue({ companyId });
        fetchNeconeWebInfo();
      }
      return;
    }
    if (userId) {
      fetchData();
    }
  }, [userId, companyId, mode]);

  const fetchData = async () => {
    if (!userId) return;
    setLoading(true);
    try {
      const [userResponse, neconeResponse] = await Promise.all([
        getPartnerUser(userId),
        getNeconeWebUser(userId).catch(() => null),
      ]);

      if (userResponse.success && userResponse.data) {
        setData(userResponse.data);
        form.setFieldsValue(userResponse.data);
      } else {
        message.error(userResponse.error?.message || 'ユーザー情報の取得に失敗しました');
      }

      if (neconeResponse?.success) {
        setNeconeWebInfo(neconeResponse.data);
      }
    } catch (error) {
      message.error('エラーが発生しました');
      console.error('Fetch error:', error);
    } finally {
      setLoading(false);
    }
  };

  const fetchNeconeWebInfo = async () => {
    if (!userId || mode !== 'new') return;
    try {
      const response = await getNeconeWebUser(userId);
      if (response.success && response.data) {
        setNeconeWebInfo(response.data);
        form.setFieldsValue({
          email: response.data.email,
          personalSms: response.data.phone,
          branchName: response.data.branchName || undefined,
        });
      }
    } catch (error) {
      console.error('NeconeWeb fetch error:', error);
    }
  };

  const handleSave = async () => {
    try {
      const values = await form.validateFields();
      setSaving(true);

      let response;
      if (mode === 'new') {
        const createData: CreatePartnerUserData = {
          userId: userId!,
          companyId: companyId!,
          notificationTarget: values.notificationTarget,
          accountType: values.accountType,
          subAccountNotification: values.accountType === 'メイン' ? values.subAccountNotification : undefined,
          branchName: values.accountType === 'サブ' ? values.branchName : undefined,
          accountStatus: values.accountStatus,
        };
        response = await createPartnerUser(createData);
      } else {
        response = await updatePartnerUser(userId!, values);
      }

      if (response.success) {
        message.success(mode === 'new' ? '登録しました' : '保存しました');
        if (mode === 'new') {
          navigate(`/b13/${userId}`);
        } else {
          setIsEditing(false);
          fetchData();
        }
      } else {
        message.error(response.error?.message || '保存に失敗しました');
      }
    } catch (error) {
      message.error('保存中にエラーが発生しました');
      console.error('Save error:', error);
    } finally {
      setSaving(false);
    }
  };

  const handleCancel = () => {
    if (mode === 'new') {
      navigate(-1);
    } else {
      setIsEditing(false);
      if (data) {
        form.setFieldsValue(data);
      }
    }
  };

  const handleAccountTypeChange = (value: 'メイン' | 'サブ') => {
    if (value === 'サブ') {
      form.setFieldsValue({ subAccountNotification: undefined });
    } else {
      form.setFieldsValue({ branchName: undefined });
    }
  };

  if (loading) {
    return <div className={styles.loading}>Loading...</div>;
  }

  return (
    <div className={styles.container}>
      <div className={styles.header}>
        <Space>
          <Button
            icon={<ArrowLeftOutlined />}
            onClick={() => navigate(-1)}
          >
            戻る
          </Button>
          <Title level={2}>
            {mode === 'new' ? '輸送パートナー新規登録（ユーザー）' : '輸送パートナー詳細（ユーザー）'}
          </Title>
        </Space>
        <Space>
          {!isEditing && mode !== 'new' && (
            <Button
              type="primary"
              icon={<EditOutlined />}
              onClick={() => setIsEditing(true)}
            >
              編集
            </Button>
          )}
          {isEditing && (
            <>
              <Button
                type="primary"
                icon={<SaveOutlined />}
                onClick={handleSave}
                loading={saving}
              >
                保存
              </Button>
              <Button
                icon={<CloseOutlined />}
                onClick={handleCancel}
              >
                キャンセル
              </Button>
            </>
          )}
        </Space>
      </div>

      {neconeWebInfo && (
        <Alert
          message="ネコーレWEBから同期した情報です"
          description={
            <Space direction="vertical">
              <Text>最終ログイン: {neconeWebInfo.lastLoginAt || 'なし'}</Text>
            </Space>
          }
          type="info"
          showIcon
          icon={<SyncOutlined />}
          className={styles.syncAlert}
        />
      )}

      <Form
        form={form}
        layout="vertical"
        disabled={!isEditing}
        initialValues={{
          notificationTarget: 'メール',
          accountType: 'サブ',
          accountStatus: '有効',
        }}
      >
        <input type="hidden" name="companyId" value={companyId || ''} />

        <Card title="ユーザー情報" className={styles.section}>
          <div className={styles.infoGrid}>
            <div className={styles.infoItem}>
              <Text type="secondary">ユーザーID</Text>
              <Text strong>{userId}</Text>
              <Form.Item name="userId" hidden>
                <Input />
              </Form.Item>
            </div>
            <div className={styles.infoItem}>
              <Text type="secondary">メール</Text>
              <Text strong>{form.getFieldValue('email') || '-'}</Text>
            </div>
            <div className={styles.infoItem}>
              <Text type="secondary">SMS</Text>
              <Text strong>{form.getFieldValue('personalSms') || '-'}</Text>
            </div>
          </div>
        </Card>

        <Card title="ユーザー設定" className={styles.section}>
          <Form.Item
            name="notificationTarget"
            label="通知送付先"
            rules={[{ required: true, message: '通知送付先を選択してください' }]}
          >
            <Radio.Group className={styles.radioGroup}>
              <Radio value="メール">
                <Text strong>● メール</Text>
                <br />
                <Text type="secondary">メールに通知を送付</Text>
              </Radio>
              <Radio value="SMS">
                <Text strong>〇 SMS</Text>
                <br />
                <Text type="secondary">SMSに通知を送付</Text>
              </Radio>
            </Radio.Group>
          </Form.Item>

          <Form.Item
            name="accountType"
            label="アカウント区分"
            rules={[{ required: true, message: 'アカウント区分を選択してください' }]}
          >
            <Radio.Group
              onChange={(e) => handleAccountTypeChange(e.target.value)}
              className={styles.radioGroup}
            >
              <Radio value="メイン">
                <Text strong>● メイン</Text>
                <br />
                <Text type="secondary">メインアカウントとして登録</Text>
              </Radio>
              <Radio value="サブ">
                <Text strong>〇 サブ</Text>
                <br />
                <Text type="secondary">サブアカウントとして登録</Text>
              </Radio>
            </Radio.Group>
          </Form.Item>

          {accountType === 'メイン' && (
            <Form.Item
              name="subAccountNotification"
              label="サブアカウントへの連絡受領"
              rules={[{ required: true, message: 'サブアカウントへの連絡受領を選択してください' }]}
            >
              <Radio.Group className={styles.radioGroup}>
                <Radio value="する">
                  <Text strong>● する</Text>
                  <br />
                  <Text type="secondary">サブアカウント宛ての連絡を受け取る</Text>
                </Radio>
                <Radio value="しない">
                  <Text strong>〇 しない</Text>
                  <br />
                  <Text type="secondary">サブアカウント宛ての連絡受け取らない</Text>
                </Radio>
              </Radio.Group>
            </Form.Item>
          )}
        </Card>

        {accountType === 'サブ' && (
          <Card title="サブアカウント設定" className={styles.section}>
            <Form.Item
              name="branchName"
              label="支店・営業所名"
              rules={[
                { required: true, message: '支店・営業所名を入力してください' },
                { max: 200, message: '支店・営業所名は200文字以内で入力してください' },
              ]}
            >
              <Input
                placeholder="支店・営業所名を入力"
                className={styles.input}
              />
            </Form.Item>
          </Card>
        )}

        <Card title="アカウント有効・無効" className={styles.section}>
          <Form.Item
            name="accountStatus"
            valuePropName="checked"
          >
            <Switch
              checkedChildren="有効"
              unCheckedChildren="無効"
              className={styles.statusSwitch}
            />
          </Form.Item>
          <Paragraph type="secondary">
            {form.getFieldValue('accountStatus') !== false
              ? '本システムを利用できる状態です'
              : '本システムを利用できない状態です'}
          </Paragraph>
        </Card>
      </Form>
    </div>
  );
};
```

### 6.2 APIクライアント

```typescript
// partnerUserApi.ts
import axios from 'axios';
import {
  GetPartnerUserRequest,
  GetPartnerUserResponse,
  UpdatePartnerUserRequest,
  UpdatePartnerUserResponse,
  CreatePartnerUserRequest,
  CreatePartnerUserResponse,
} from '../types/partnerUser';

const API_BASE_URL = '/api/v1';

const apiClient = axios.create({
  baseURL: API_BASE_URL,
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json',
  },
});

apiClient.interceptors.request.use((config) => {
  const token = localStorage.getItem('auth_token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

apiClient.interceptors.response.use(
  (response) => response.data,
  (error) => {
    if (error.response?.status === 401) {
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);

export const getPartnerUser = async (
  userId: string
): Promise<GetPartnerUserResponse> => {
  return apiClient.get(`/partner-users/${userId}`);
};

export const updatePartnerUser = async (
  userId: string,
  data: UpdatePartnerUserRequest['updateData']
): Promise<UpdatePartnerUserResponse> => {
  return apiClient.put(`/partner-users/${userId}`, { updateData: data });
};

export const createPartnerUser = async (
  data: CreatePartnerUserRequest
): Promise<CreatePartnerUserResponse> => {
  return apiClient.post('/partner-users', data);
};
```

---

## 7. 后端服务设计

### 7.1 Controller

```java
@RestController
@RequestMapping("/api/v1/partner-users")
@RequiredArgsConstructor
@Validated
public class PartnerUserController {

    private final PartnerUserService partnerUserService;
    private final NeconeWebService neconeWebService;

    @GetMapping("/{userId}")
    @ResponseStatus(HttpStatus.OK)
    public ResponseEntity<GetPartnerUserResponse> getUserDetail(
            @PathVariable @NotBlank String userId) {
        GetPartnerUserResponse response = partnerUserService.getUserDetail(userId);
        return ResponseEntity.ok(response);
    }

    @PutMapping("/{userId}")
    @ResponseStatus(HttpStatus.OK)
    public ResponseEntity<UpdatePartnerUserResponse> updateUser(
            @PathVariable @NotBlank String userId,
            @Valid @RequestBody UpdatePartnerUserRequest request) {
        UpdatePartnerUserResponse response = partnerUserService.updateUser(
            userId, request.getUpdateData());
        return ResponseEntity.ok(response);
    }

    @PostMapping
    @ResponseStatus(HttpStatus.CREATED)
    public ResponseEntity<CreatePartnerUserResponse> createUser(
            @Valid @RequestBody CreatePartnerUserRequest request) {
        CreatePartnerUserResponse response = partnerUserService.createUser(request);
        return ResponseEntity.ok(response);
    }
}
```

### 7.2 Service

```java
@Service
@RequiredArgsConstructor
@Slf4j
public class PartnerUserService {

    private final PartnerUserRepository partnerUserRepository;
    private final PartnerCompanyRepository partnerCompanyRepository;

    @Transactional(readOnly = true)
    public GetPartnerUserResponse getUserDetail(String userId) {
        PartnerUser user = partnerUserRepository.findById(userId)
                .orElseThrow(() -> new UserNotFoundException(userId));

        return GetPartnerUserResponse.builder()
                .success(true)
                .data(PartnerUserData.builder()
                        .userId(user.getUserId())
                        .email(user.getEmail())
                        .personalSms(user.getPersonalSms())
                        .notificationTarget(user.getNotificationTarget().getValue())
                        .accountType(user.getAccountType().getValue())
                        .subAccountNotification(user.getSubAccountNotification()?.getValue())
                        .branchName(user.getBranchName())
                        .accountStatus(user.getAccountStatus().getValue())
                        .companyId(user.getCompanyId())
                        .createdAt(user.getCreatedAt().toString())
                        .updatedAt(user.getUpdatedAt().toString())
                        .build())
                .build();
    }

    @Transactional
    public UpdatePartnerUserResponse updateUser(
            String userId, UpdatePartnerUserRequest.UpdateData updateData) {
        PartnerUser user = partnerUserRepository.findById(userId)
                .orElseThrow(() -> new UserNotFoundException(userId));

        if (updateData.getNotificationTarget() != null) {
            user.setNotificationTarget(
                NotificationTarget.fromValue(updateData.getNotificationTarget()));
        }

        if (updateData.getAccountType() != null) {
            AccountType newType = AccountType.fromValue(updateData.getAccountType());
            AccountType oldType = user.getAccountType();

            if (newType != oldType) {
                if (newType == AccountType.メイン) {
                    user.setSubAccountNotification(updateData.getSubAccountNotification() != null
                        ? SubAccountNotification.fromValue(updateData.getSubAccountNotification())
                        : null);
                    user.setBranchName(null);
                } else {
                    user.setSubAccountNotification(null);
                    user.setBranchName(updateData.getBranchName());
                }
            } else {
                if (newType == AccountType.メイン && updateData.getSubAccountNotification() != null) {
                    user.setSubAccountNotification(
                        SubAccountNotification.fromValue(updateData.getSubAccountNotification()));
                } else if (newType == AccountType.サブ && updateData.getBranchName() != null) {
                    user.setBranchName(updateData.getBranchName());
                }
            }
            user.setAccountType(newType);
        }

        if (updateData.getAccountStatus() != null) {
            user.setAccountStatus(AccountStatus.fromValue(updateData.getAccountStatus()));
        }

        partnerUserRepository.save(user);

        log.info("パートナーユーザー {} の情報が更新されました", userId);

        return UpdatePartnerUserResponse.builder()
                .success(true)
                .message("保存しました")
                .build();
    }

    @Transactional
    public CreatePartnerUserResponse createUser(CreatePartnerUserRequest request) {
        if (partnerUserRepository.existsById(request.getUserId())) {
            throw new DuplicateUserException(request.getUserId());
        }

        partnerCompanyRepository.findById(request.getCompanyId())
                .orElseThrow(() -> new CompanyNotFoundException(request.getCompanyId()));

        PartnerUser user = PartnerUser.builder()
                .userId(request.getUserId())
                .companyId(request.getCompanyId())
                .email(extractEmailFromUserId(request.getUserId()))
                .personalSms(extractPhoneFromUserId(request.getUserId()))
                .notificationTarget(NotificationTarget.fromValue(request.getNotificationTarget()))
                .accountType(AccountType.fromValue(request.getAccountType()))
                .subAccountNotification(request.getAccountType().equals("メイン")
                    ? SubAccountNotification.fromValue(request.getSubAccountNotification())
                    : null)
                .branchName(request.getAccountType().equals("サブ")
                    ? request.getBranchName()
                    : null)
                .accountStatus(AccountStatus.fromValue(request.getAccountStatus()))
                .build();

        partnerUserRepository.save(user);

        log.info("新規パートナーユーザー {} が作成されました", request.getUserId());

        return CreatePartnerUserResponse.builder()
                .success(true)
                .message("登録しました")
                .data(CreateUserResult.builder()
                        .userId(user.getUserId())
                        .createdAt(user.getCreatedAt().toString())
                        .build())
                .build();
    }
}
```

---

## 8. 数据库表设计

### 8.1 partner_usersテーブル

```sql
CREATE TABLE partner_users (
    user_id VARCHAR(50) NOT NULL COMMENT 'ユーザーID',
    company_id VARCHAR(50) NOT NULL COMMENT '会社ID',
    email VARCHAR(200) NOT NULL COMMENT 'メールアドレス',
    personal_sms VARCHAR(20) NOT NULL COMMENT 'SMS電話番号',
    notification_target ENUM('メール', 'SMS') NOT NULL DEFAULT 'メール' COMMENT '通知送付先',
    account_type ENUM('メイン', 'サブ') NOT NULL DEFAULT 'サブ' COMMENT 'アカウント区分',
    sub_account_notification ENUM('する', 'しない') COMMENT 'サブアカウントへの連絡受領',
    branch_name VARCHAR(200) COMMENT '支店・営業所名',
    account_status ENUM('有効', '無効') NOT NULL DEFAULT '有効' COMMENT 'アカウント状態',
    password_hash VARCHAR(255) COMMENT 'パスワードハッシュ',
    last_login_at TIMESTAMP COMMENT '最終ログイン日時',
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    PRIMARY KEY (user_id),
    
    INDEX idx_users_company_id (company_id),
    INDEX idx_users_account_status (account_status),
    INDEX idx_users_account_type (account_type),
    INDEX idx_users_notification_target (notification_target),
    
    FOREIGN KEY (company_id) REFERENCES partner_companies(company_id) ON DELETE CASCADE,
    
    CONSTRAINT chk_user_email_unique UNIQUE (email)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='ユーザーマスタテーブル';
```

### 8.2 partner_user_audit_logテーブル

```sql
CREATE TABLE partner_user_audit_log (
    id BIGSERIAL PRIMARY KEY,
    user_id VARCHAR(50) NOT NULL COMMENT 'ユーザーID',
    action VARCHAR(50) NOT NULL COMMENT '操作区分',
    before_value TEXT COMMENT '変更前値',
    after_value TEXT COMMENT '変更後値',
    performed_by VARCHAR(100) NOT COMMENT '実行者',
    performed_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    
    INDEX idx_audit_user_id (user_id),
    INDEX idx_audit_performed_at (performed_at),
    
    FOREIGN KEY (user_id) REFERENCES partner_users(user_id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='ユーザー操作監査ログテーブル';
```

---

## 9. 验收标准

### 9.1 功能验收

| 测试场景 | 预期结果 | 优先级 |
|----------|----------|--------|
| 页面初始加载（既存用户） | 显示用户信息、设置、状态 | P0 |
| 页面初始加载（新规用户） | 显示空表单，companyId已设定 | P0 |
| 页面URL访问 | 正确获取userId并加载数据 | P0 |
| ネコーレWEB連携 | 自动获取邮件、电话信息 | P0 |
| メール/SMS選択 | 通知送付先正确保存 | P0 |
| メイン/サブ選択 | 根据选择显示/隐藏相关字段 | P0 |
| フィールド条件表示 | 条件フィールド正确显示/隐藏 | P0 |
| 保存按钮点击 | 验证并提交更新 | P0 |
| 取消按钮点击 | 恢复原始值，退出编辑模式 | P0 |
| 有効/無効切换 | 状态正确切换 | P0 |
| 返回按钮点击 | 返回前一页面 | P1 |
| バリデーション | 必填项、格式验证 | P0 |

### 9.2 视觉验收

| 项目 | 要求 |
|------|------|
| 布局 | 与设计稿一致 |
| 颜色 | 状态标签颜色正确 |
| 间距 | 符合UI规范 |
| 字体 | 大小、颜色正确 |
| 条件显示 | 满足条件时正确显示/隐藏 |

### 9.3 性能验收

| 指标 | 要求 | 测试方法 |
|------|------|----------|
| 页面加载 | <2秒 | Lighthouse |
| API响应 | <500ms | API监控 |
| 外部連携 | <2秒 | API监控 |

### 9.4 安全验收

| 检查项 | 要求 |
|--------|------|
| 权限控制 | 验证用户是否有权限访问/修改该用户 |
| CSRF防护 | 所有PUT/POST请求带Token |
| SQL注入 | 使用参数化查询 |
| 数据验证 | 后端二次验证 |

---

## 10. 变更日志

| 日期 | 版本 | 变更内容 | 作者 |
|------|------|---------|------|
| 2026-02-10 | v1.0.1 | 添加5.6画面-API-数据库关系映射表 | - |
| 2026-02-XX| v1.0 | 初始版本 | - |

---

## 附录

### A.1 相关文档

| 文档 | 路径 | 说明 |
|------|------|------|
| 公共技术架构规范 | [\_公共技术架构规范.md](../_公共技术架构规范.md) | 前后端技术架构详细规范 |
| b12-輸送パートナーマスタ詳細（会社） | [../b12-輸送パートナーマスタ詳細（会社）/05_设计规范.md](../b12-輸送パートナーマスタ詳細（会社）/05_设计规范.md) | 公司详情页面 |
| b11-輸送パートナーマスタ一覧 | [../b11-輸送パートナーマスタ一覧/05_设计规范.md](../b11-輸送パートナーマスタ一覧/05_设计规范.md) | 合作伙伴公司一览 |
| p0-共通ナビゲーション | [../p0-共通ナビゲーション/05_设计规范.md](../p0-共通ナビゲーション/05_设计规范.md) | 合作伙伴导航页面 |

### A.2 関連画面遷移

| 画面 | 遷移元 | 遷移先 | トリガー | 説明 |
|------|--------|--------|----------|------|
| b13 | b12一覧 | - | - | 一覧から選択 |
| b13 | b12 | - | ＋新規追加 | 新規ユーザー登録 |
| b13 | - | b12 | 戻るボタン | 会社詳細へ戻る |

### A.3 业务规则

| 规则名称 | 规则说明 | 验证时机 |
|----------|----------|----------|
| メインアカウント必須 | 同一会社内にメインアカウントが1つ必要 | サブ→メイン変換時 |
| 会社存在チェック | 会社は事前に登録されていること | 新規登録時 |
| 重複チェック | ユーザーIDは既存と重複不可 | 新規登録時 |
| メール形式 | メールは有効なemail形式であること | バリデーション |

### A.4 API调用示例

```bash
# ユーザー情報取得
curl -X GET "https://api.example.com/api/v1/partner-users/U001" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
```

```bash
# ユーザー情報更新
curl -X PUT "https://api.example.com/api/v1/partner-users/U001" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." \
  -H "Content-Type: application/json" \
  -d '{
    "updateData": {
      "notificationTarget": "SMS",
      "accountStatus": "有効"
    }
  }'
```

```bash
# ユーザー新規作成
curl -X POST "https://api.example.com/api/v1/partner-users" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." \
  -H "Content-Type: application/json" \
  -d '{
    "userId": "U002",
    "companyId": "C001",
    "notificationTarget": "メール",
    "accountType": "サブ",
    "branchName": "東京支社",
    "accountStatus": "有効"
  }'
```

---

**文档编制**: -  
**编制时间**: 2026-02-XX 
**适用范围**: b13-輸送パートナー詳細（ユーザー）合作伙伴用户详情页面  
**交叉引用**: [\_公共技术架构规范.md](../_公共技术架构规范.md)
