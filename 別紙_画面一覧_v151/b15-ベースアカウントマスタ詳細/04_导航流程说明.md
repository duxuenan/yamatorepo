# b15-ベースアカウントマスタ詳細 - 导航流程说明

## 页面在系统中的位置

```
配車発注管理システム
├── 共通ナビゲーション (b0)
│   ├── 配車発注管理
│   │   ├── 発注管理
│   │   │   ├── 発注検索 (b2)
│   │   │   └── 発注詳細 (b3)
│   │   ├── 実績管理
│   │   │   ├── 実績検索 (b4)
│   │   │   └── 実績詳細 (b5)
│   │   ├── 被請求管理
│   │   │   ├── 被請求管理 (b6)
│   │   │   └── 被請求詳細 (b7)
│   │   └── CSVアップロード (b8)
│   ├── アクション履歴 (b9)
│   ├── マスタ管理
│   │   ├── 輸送パートナーマスタ検索 (b11)
│   │   ├── b12-輸送パートナーマスタ詳細（会社）
│   │   ├── b13-輸送パートナー詳細（ユーザー）
│   │   ├── b14-ベースアカウントマスタ検索
│   │   └── b15-ベースアカウントマスタ詳細 (当前页面)
│   └── ログアウト
```

## 进入当前页面的导航路径

### 路径1：从搜索页面进入（点击用户ID）

```
用户访问 b14 页面
  ↓
点击账户列表中的用户ID链接
  ↓
进入 b15-ベースアカウントマスタ詳細 页面
```

### 路径2：从搜索页面进入（新增用户）

```
用户访问 b14 页面
  ↓
点击「＋新規追加」按钮
  ↓
进行用户搜索
  ↓
点击「決定」按钮
  ↓
进入 b15-ベースアカウントマスタ詳細 页面（新增模式）
```

## 从当前页面出去的导航路径

### 跳转到其他页面

| 目标页面 | 触发条件 | 跳转方式 |
|----------|----------|----------|
| b14-ベースアカウントマスタ検索 | 点击浏览器返回按钮 | 返回 |
| b0-共通ナビゲーション | 点击左侧菜单其他模块 | 直接跳转 |

## 面包屑导航

```
ホーム > マスタ管理 > アカウントマスタ管理 > ベースアカウントマスタ詳細
```

## 用户操作流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户操作流程                              │
└─────────────────────────────────────────────────────────────────┘

[开始]
  │
  ├─► 访问 b14 页面
  │     │
  │     ├─► 点击用户ID链接
  │     │       │
  │     │       └─► [进入 b15 页面 - 编辑模式]
  │     │                │
  │     │                ├─► [查看用户信息]
  │     │                │      │
  │     │                │      └─► 显示只读信息（不可编辑）
  │     │                │
  │     │                ├─► [编辑所属]
  │     │                │      │
  │     │                │      └─► 从ベース一覧选择
  │     │                │
  │     │                ├─► [设置权限]
  │     │                │      │
  │     │                │      └─► 选择权限类型
  │     │                │
  │     │                └─► [切换账户状态]
  │     │                           │
  │     │                           └─► 切换有效/无效
  │     │
  │     └─► 点击「＋新規追加」
  │              │
  │              └─► [进入 b15 页面 - 新增模式]
  │                       │
  │                       ├─► 预填用户信息（AzureAD）
  │                       ├─► 设置所属
  │                       ├─► 设置权限
  │                       └─► 点击「保存」
  │
  └─► [结束]
```

## 页面状态管理

### URL参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
| user_id | string | 用户ID |
| mode | string | edit / add |

### 页面状态

| 状态类型 | 状态值 | 说明 |
|----------|--------|------|
| mode | view | 浏览模式 |
| mode | edit | 编辑模式 |
| mode | add | 新增模式 |
| source | azure_ad | 数据来源：AzureAD |
| source | local | 数据来源：本地 |
